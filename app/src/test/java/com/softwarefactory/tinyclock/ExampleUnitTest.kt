package com.softwarefactory.tinyclock

import org.junit.Assert.assertEquals
import org.junit.Test

class ExampleUnitTest {
    @Test
    fun projectNameIsInjected() {
        assertEquals("Tiny Clock", "Tiny Clock")
    }
}
