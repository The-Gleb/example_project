# Example Project 3

This repository is the `example_project` Android skeleton, initialized from the `android-only` profile.

The skeleton ships with Kotlin + Jetpack Compose, targets JDK 21, and is built with Gradle. No backend module is included.

Further features will be added incrementally by the Software Factory platform.
